import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Magazinify AI™ — AI-Powered Digital Magazines for Your Business",
  description:
    "Transform your business into a professional monthly digital magazine. AI-generated content, SEO-optimized articles, and a polished flipbook — delivered every month.",
  keywords: ["digital magazine", "AI content", "business marketing", "SEO", "monthly publication"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
